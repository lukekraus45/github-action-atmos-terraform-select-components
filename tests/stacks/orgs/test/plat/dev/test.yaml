import:
  - orgs/test/plat/_defaults

vars:
  stage: dev

components:
  terraform:
    test-3:
      vars:     
        seed: 6
    test-4:
      settings:
        github:
          actions_enabled: true
      vars:           
        seed: 2
