import:
  - orgs/test/plat/_defaults

vars:
  stage: sandbox

components:
  terraform:
    test-5:
      settings:
        github:
          actions_enabled: true
      vars:           
        seed: 3
