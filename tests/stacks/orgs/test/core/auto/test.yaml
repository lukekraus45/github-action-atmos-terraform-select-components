import:
  - orgs/test/core/_defaults

vars:
  stage: auto

components:
  terraform:
    test-1:
      vars: 
        seed: 6
    test-2:
      settings:
        github:
          actions_enabled: true
      vars:           
        seed: 2
